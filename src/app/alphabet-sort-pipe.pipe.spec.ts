import { AlphabetSortPipePipe } from './alphabet-sort-pipe.pipe';

describe('AlphabetSortPipePipe', () => {
  it('create an instance', () => {
    const pipe = new AlphabetSortPipePipe();
    expect(pipe).toBeTruthy();
  });
});
